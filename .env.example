# Supabase (server-side only — never expose these to the browser)
SUPABASE_URL=
SUPABASE_KEY=

# Session signing secret (random 64-char hex)
AUTH_SECRET=

# Shared role passwords
ADMIN_PASSWORD=
MANAGER_PASSWORD=
REFEREE_PASSWORD=
OPERATOR_PASSWORD=
